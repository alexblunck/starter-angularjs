/**
 * Module
 * app.filters
 */

import angular from 'angular'

// import filter from './filter'

angular
    .module('app.filters', [])
    // .filter('filter', filter)

export default 'app.filters'
